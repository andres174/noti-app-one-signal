import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: '04AppNotificaciones',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
